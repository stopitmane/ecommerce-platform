{
  "version": 2,
  "buildCommand": "npm install -w client && npm run build -w client && mkdir -p dist && cp -r client/dist/* dist/",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
