{
  "version": 2,
  "buildCommand": "npm install --workspace=client && npm install --workspace=server && npm run build --workspace=client && cp -r client/dist dist",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
