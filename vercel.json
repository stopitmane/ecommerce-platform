{
  "version": 2,
  "buildCommand": "npm install --workspace=client && npm install --workspace=server && npm run build --workspace=client",
  "outputDirectory": "client/dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
