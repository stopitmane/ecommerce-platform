{
  "version": 2,
  "buildCommand": "npm install --prefix client && npm run build --prefix client && mkdir -p dist && cp -r client/dist/* dist/",
  "outputDirectory": "dist",
  "installCommand": "npm install && npm install --prefix server",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
